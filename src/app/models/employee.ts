// create an employee interface with the following properties: id, firstName, lastName, contactNumber, and project.
export interface Employee {
  id?: string;
  firstName: string;
  lastName: string;
  contactNumber: string;
  project: string;
}
